<%= form_with(model: favorite) do |form| %>
  <% if favorite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(favorite.errors.count, "error") %> prohibited this favorite from being saved:</h2>

      <ul>
        <% favorite.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :favorable_id %>
    <%= form.text_field :favorable_id %>
  </div>

  <div class="field">
    <%= form.label :favorites_list_id %>
    <%= form.text_field :favorites_list_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
