<%= form_with(model: favorites_list) do |form| %>
  <% if favorites_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(favorites_list.errors.count, "error") %> prohibited this favorites_list from being saved:</h2>

      <ul>
        <% favorites_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
